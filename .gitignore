# npm dependencies
node_modules/
# Built files
build/
# VS code workspace
*.code-workspace
# istanbul output
coverage/
.nyc_output

[alias]
	sync-ghpages = !git checkout gh-pages && git rebase master && git push origin gh-pages && git checkout master